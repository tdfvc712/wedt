apiVersion: v1
kind: Service
metadata:
  name: mioke
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mioke"
    port: 8080
  selector:
    app: mioke
